# Core
pyyaml>=6.0
requests>=2.31.0
python-dotenv>=1.0.0  # loads TAVILY_API_KEY (and other secrets) from a .env file

# PDF extraction
pypdf>=4.0.0
pdfplumber>=0.11.0
PyMuPDF>=1.24.0      # fitz - used for rasterizing slides to images

# Folder watching
watchdog>=4.0.0

# CLI
click>=8.1.0

# Logging niceties (optional but nice)
rich>=13.7.0
